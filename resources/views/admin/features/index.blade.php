@extends('admin.layout')
@section('title', 'Features')
@section('admin-content')<h1>Features</h1><details class="platform-card"><summary>{{ __('platform.add_feature_metadata') }}</summary><form class="form-grid" method="POST" action="{{ route('admin.features.store') }}">@csrf<label>Code<input name="code" pattern="[a-z][a-z0-9_]{2,79}" required></label><label>Name<input name="name" required></label><label>Category<input name="category" value="general" required></label><label>Description<textarea name="description"></textarea></label><label>Credit cost<input type="number" name="credit_cost" value="0" min="0"></label><label>Sort order<input type="number" name="sort_order" value="0" min="0"></label><label class="checkbox-label"><input type="checkbox" name="is_visible" value="1" checked> Visible</label><button>{{ __('platform.save') }}</button></form></details><div class="platform-grid">@foreach($features as $feature)<form class="platform-card form-grid @unless($registry->has($feature->code)) locked @endunless" method="POST" action="{{ route('admin.features.update', $feature) }}">@csrf @method('PUT')<h2>{{ $feature->code }}</h2><span class="badge {{ $registry->has($feature->code) ? 'safe' : 'warning' }}">{{ $registry->has($feature->code) ? 'Registered' : 'Not registered' }}</span><label>Name<input name="name" value="{{ $feature->name }}" required></label><label>Description<textarea name="description">{{ $feature->description }}</textarea></label><label>Category<input name="category" value="{{ $feature->category }}" required></label><label>Cost<input type="number" name="credit_cost" value="{{ $feature->credit_cost }}" min="0" required></label><label>Order<input type="number" name="sort_order" value="{{ $feature->sort_order }}" min="0" required></label><label class="checkbox-label"><input type="checkbox" name="is_enabled" value="1" @checked($feature->is_enabled)> Enabled</label><label class="checkbox-label"><input type="checkbox" name="is_visible" value="1" @checked($feature->is_visible)> Visible</label><fieldset><legend>Plans</legend>@foreach($plans as $plan)<label class="checkbox-label"><input type="checkbox" name="plans[]" value="{{ $plan->id }}" @checked($feature->plans->contains($plan))>{{ $plan->name }}</label>@endforeach</fieldset><button>{{ __('platform.save') }}</button></form>@endforeach</div>@endsection
